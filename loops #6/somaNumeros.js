/*
Use um loop for para calcular a soma dos números de 1 a 100.
*/

let total = 0;

for(let number = 1; number <= 100; number++) {
    total = total + number;  //A variável `total` acumula o resultado das somas
    console.log(total);
}
