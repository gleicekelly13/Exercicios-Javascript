/*function apertar () {
    console.log("APERTOU!");
}

function segurar () {
    console.log("SEGUROU!");
}  */

/*function soltar (e) {
    console.log('TECLA APERTADA: ' + e.code);
    console.log('SHIFT' + e.shiftKey);
}

const input =document.querySelector('input');
input.addEventListener('keyup', soltar); //Aplica o evento diretamente no input 

/*document.addEventListener('keyup', soltar); //funciona em qualquer lugar do documento */
